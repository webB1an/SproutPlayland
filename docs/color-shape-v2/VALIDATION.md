# 验证记录

## 已完成

- 检查颜色与形状配置能够驱动实际游戏内容；
- 检查独立 Piece / Target 匹配模型；
- 检查同 `matchKey` 的目标互换；
- 检查错误回位、目标悬停反馈和全部完成回调；
- 检查 4/6/8 个形状积木和 3/4/5 个颜色目标的配置分支；
- 检查分支只修改颜色、形状和公共匹配模块，没有直接改动 `main`。

## 仍需 Creator 环境验证

当前执行环境没有 Cocos Creator 3.8.8，因此尚未执行：

- Creator 资源导入；
- Web Preview 实际编译；
- 微信小游戏构建；
- Android/iOS 真机触摸测试；
- 音效、语音、振动和低性能设备帧率测试。

建议优先验证：

1. 4/6/8 个形状积木在 16:9 与更宽屏幕上的托盘间距；
2. 同 `matchKey` 部件互换后是否正确隐藏目标槽位；
3. 刘海屏上的返回和帮助按钮；
4. 完成动画结束后奖励弹窗是否只出现一次；
5. 微信小游戏首次进入颜色/形状模块时的资源加载耗时。
